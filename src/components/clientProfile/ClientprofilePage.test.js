import React from 'react';
import expect from 'expect';
import {mount, shallow} from 'enzyme';
import ClientprofilePage from './ClientprofilePage';
import { wrap } from 'module';
/*
describe('Testing ClientprofilePage functionality', () =>{

  // set up
  var wrapper = mount(<ClientprofilePage />);

  it('Default targets amount is 0', () =>{
    expect(wrapper.state().targetNames.length).toBe(0);
  });

  it('One Add target should be default displayed', () =>{
    expect(wrapper.state().AddTarget).toEqual(true);
  });



});*/